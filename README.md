# Google-Fiber-Project
Ingested three separate market datasets into BigQuery, merged them into a single reporting table using UNION ALL, exported the results as a CSV, and built an interactive Tableau dashboard to visualise repeat caller trends for the Google Fiber customer service team.
